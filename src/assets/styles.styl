@require './smartgrid.styl'

html
body
#app
  margin 0
  min-height 100vh

body
  -webkit-font-smoothing antialiased
  -moz-osx-font-smoothing grayscale
  font-family Roboto, sans-serif
  background-color #152347
  color #99a1b3

#app
  display flex
  flex-direction column
  min-width 320px

.l-wrapper
  wrapper()
